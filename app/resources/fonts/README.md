To enable better Arabic typography, download the Noto Sans Arabic Regular .ttf file (from https://fonts.google.com/specimen/Noto+Sans+Arabic) and place it into this directory as:

    app/resources/fonts/NotoSansArabic-Regular.ttf

The app will automatically load it when Arabic is selected in Settings. If the file is missing, a small notice will appear in the Settings page with instructions.