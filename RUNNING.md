تشغيل التطبيق (Windows)

الخيار الموصى به: استخدم السكربت الجاهز `run.bat` أو `run.ps1` في جذر المشروع. السكربت سيقوم تلقائياً بإنشاء بيئة افتراضية `.venv` وتثبيت الحزم من `requirements.txt` إذا لم تكن مُثبتة، ثم سيشغل التطبيق.

الاستخدام:
- عبر موجه الأوامر (cmd): `run.bat`
- عبر PowerShell: `.


ملاحظة: إذا أردت ملف تنفيذي مستقل (بدون تثبيت Python/المكتبات على الجهاز)، أخبرني لأعدّ سكربت `PyInstaller` لبناء exe.

🔧 ترجمة التطبيق:
- الملفات المصدرية للتّرجمات موجودة في `app/translations/<lang>/LC_MESSAGES/messages.po`.
- لتجميع ملفات `.po` إلى `.mo` (التي يستخدمها gettext) شغّل: `python scripts/compile_translations.py`.

🎨 أيقونات:
- توجد نسخ أحادية اللون من الأيقونات في `app/resources/icons/*_mono.svg`. هذه النسخ تُستخدم عند الحاجة لتلوين الأيقونات طبقًا للثيم.`